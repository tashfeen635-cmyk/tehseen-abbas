# Admin session signing secret - set a long random string
ADMIN_SECRET=change-this-to-a-long-random-string

# Optional: override admin username/password from the DB.
# When both are set, these take precedence over the database credentials.
ADMIN_USER=admin
ADMIN_PASSWORD=secret
